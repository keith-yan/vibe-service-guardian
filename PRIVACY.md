# 隐私说明

Vibe Service Guardian 是单机本地工具，不包含遥测、账号系统、云端存储、广告 SDK 或外部 CDN。Web 控制台只监听 `127.0.0.1`。

## 读取的数据

- 本机监听/绑定端口、PID、进程名、可执行路径、当前工作目录、父进程链、启动时间和资源占用；
- 已脱敏的进程命令参数；
- 用户配置的项目根目录和项目标记文件是否存在；
- 当前用户可访问的近期 Codex/Claude 会话元数据；
- Hermes/OpenCode 会话数据库中的已核验元数据列；
- Gemini 会话文件名和修改时间，不打开聊天文件；
- Docker 与 WSL CLI 返回的运行实体、端口摘要和 Docker 重启策略/计数。
- CPU/核心数、总/可用内存、系统盘容量、GPU 型号/显存/驱动版本、Apple 芯片与本地推理运行时是否存在。
- 实时 CPU/RAM/交换空间、系统卷与项目卷余量、网卡总量/速率，以及系统或厂商工具实际暴露的 GPU 利用率、显存分配、温度、风扇和功耗。拿不到的传感器保持不可用。
- 已识别模型进程的当前远端 IP、端口、协议、连接状态和公网/私网作用域；不读取包正文、DNS 名称或 URL。
- 已识别本机模型服务的无凭据只读健康接口和 metrics 返回：加载模型、量化、上下文、并发/KV、TPS/TTFT、队列和认证响应状态。响应体有大小/超时限制，不发送用户内容。
- 用户主动提交的模型容量工作负载；它只用于当前本地请求和操作审计中的并发/上下文/速度摘要。
- 用户明确确认后运行的 `llama-bench` 数值结果。
- 用户输入 `BENCHMARK <端口>` 后，对本机模型服务发送的固定合成提示及其性能数值；生成正文不持久化。
- 用户预览并输入 `BENCHMARK PLAN <端口>` 后运行的固定三步服务矩阵；保存负载参数、样本数、TPS/TTFT、资源采样峰值及可选预测误差，不保存合成提示或生成正文。
- 用户输入 `STOP <PID>` 后有限窗口内的原 PID/子进程、原端口和替代 PID 验证结果；验证器不自动结束替代进程。
- 用户输入 `INSPECT <PID>` 并选择的日志尾部或配置文件；结果在返回前脱敏且不写数据库。
- 用户输入 `WATCH <PID>` 后明确选择的模型服务日志；持续监控只增量读取该普通文件并保存脱敏结构化事件。原始行不入库，绝对路径仅保存在本机私有监控游标中且不通过 Web API 返回。
- 用户输入 `SNAPSHOT` 后指定文件的本地清单；小型常见配置可按原内容复制到数据目录用于显式回滚。
- 设置中人工添加的受信回环/私网节点 URL；只访问其 `/healthz` 或 `/health`，不携带凭据。
- 用户明确选择并输入 `SCAN MODELS` 的本地模型目录；只在有界范围内读取文件名、大小、修改时间、首尾快速指纹以及 GGUF/Safetensors/Ollama/Modelfile/config 元数据。
- 用户创建的本地归属规则和项目 `.vsg.yaml` 中的服务声明；清单不执行，只读取受支持字段。
- 用户在服务详情主动选择的“确属遗留 / 不是遗留 / 暂不确定”结果，以及判断当时的风险等级/分数和来源分组；不保存该确认的自由文本。

程序不会枚举目标进程的环境变量。程序自身只使用路径/定位变量 `VSG_DATA_DIR`、`CODEX_HOME`、`XDG_DATA_HOME`、`LOCALAPPDATA`、`SystemDrive` 和 `PATH`，以确定本地数据目录、系统卷和本地可执行文件；不会把这些变量值发送到网络或显示在模型规划页面。构建辅助脚本还可读取 `VSG_PYTHON_LICENSE` 与 `SOURCE_DATE_EPOCH`。

## 不读取的数据

- Agent 提示词、回复或聊天正文；
- 浏览器 Cookie、登录态、密码库或剪贴板；
- 网络包正文、DNS 名称、URL 路径和应用层内容；
- API Key、Token 或密码的环境变量值。
- 用户未明确选择的模型目录、日志、配置或模型文件清单；不会自动扫描这些路径。

命令行仍可能包含敏感值，因此在进入快照前会对常见密钥参数、凭据 URI、查询参数和供应商 Token 做脱敏。无法保证识别所有自定义密钥格式；不要把秘密直接放在命令行中。

## 本地持久化

默认数据目录是程序目录下的 `data/`，也可通过 `--data-dir` 或 `VSG_DATA_DIR` 修改。文件包括：

- `config.json`：项目根目录和扫描设置；
- `history.sqlite3`：服务指纹、首次/最后观察时间、项目路径、Agent/会话归属、用户标记和操作审计；
- `vsg.log`：程序错误和运行状态，不记录请求体；
- `runtime.json`：当前实例的 PID、回环端口和启动时间。
- `snapshots/`：用户显式创建的文件清单和小配置副本。

`history.sqlite3` 还可保存本地归属规则、模型盘点历史、生命周期时间线和 15 秒遥测样本。模型盘点只保存根目录名称/摘要和相对路径，不保存绝对扫描根。当前远端 IP 不写入时间线；历史连接事件只保存不可逆端点摘要、作用域、协议和端口。

`impact_feedback` 每个服务指纹最多保存一条可更新结果、创建/更新时间、判断时风险等级/分数和来源分组。控制台成效报告只读取聚合计数，不导出服务指纹。报告固定排除 PID、路径、IP、命令、会话 ID、日志和模型响应，并声明它不是独立用户或公开采用证据。报告仅在用户输入 `EXPORT REPORT` 后由浏览器下载，不自动上传。

历史库不保存完整命令，只保存脱敏命令的 SHA-256。默认历史保留 14 天。POSIX 系统会尽力将数据目录设为 `0700`、文件设为 `0600`；不支持 POSIX 权限的共享/移动介质可能无法强制这些模式。Windows 文件继承所在目录 ACL，因此不应把便携包放在其他用户可读取或写入的共享目录。

如果用户主动运行模型基准，`history.sqlite3` 的 `model_benchmarks` 表会保存硬件指纹、目录模型 ID、量化、模型**文件名**、文件大小、提示/生成速度和已筛选的数值记录。绝对路径不会持久化；硬件指纹由平台、CPU、内存总量和 GPU 名称/容量哈希生成，不含序列号、MAC、主机名或用户名。模型基准与服务观察使用同一历史保留天数自动清理；用户也可通过分类清除或删除整个 `data/` 目录移除。

模型服务短基准和固定矩阵记录位于 `service_benchmarks`，只保存服务/硬件指纹、运行时、模型标识、可选目录模型 ID/量化、并发、上下文、输出、样本数、TPS/TTFT、资源峰值、预测误差、已核实 token 数与错误分类，不保存提示词、生成正文、模型绝对路径或秘密。停止后的有限窗口验证位于 `stop_verifications`，保存原 PID、端口关闭状态、存活/替代 PID 和结果分类；不会保存远端地址或客户端内容。

持续日志监控记录位于 `log_watches` 与 `log_events`。`log_watches` 为读取游标在本机保存用户明确选择的绝对路径、文件名、服务指纹、PID、启动时间和字节偏移；Web API 会移除绝对路径。`log_events` 只保存脱敏后的最多 500 字符事件、类别、严重度、首次/末次时间和重复次数，不保存原始日志。事件默认保留 7 天，可设置为 1–90 天；停用游标不会删除已有事件，删除整个 `data/` 可完全清除。

文件快照的 `manifest.json` 会在本地记录原始绝对路径，Web 历史列表只返回文件名。已知 `.env`、凭据、私钥、证书和密钥库文件只记录清单、不会复制；其他被允许的小配置副本是**原样副本**，不会自动脱密，仍可能包含自定义或未识别的秘密。快照只在用户输入 `SNAPSHOT` 后创建，应把数据目录放在仅当前用户可读的位置。`INSPECT` 检查与快照是两条不同链路：前者返回脱敏内容但不备份，后者为回滚保真而保存获准文件原文。

## 删除数据

控制台可在用户精确输入 `CLEAR HISTORY` 后，按类别删除服务观察、审计、模型/服务基准、停止验证、成效确认、脱敏日志事件、时间线、遥测样本或模型盘点历史；这不会删除原始模型、项目、日志、配置文件、归属规则、日志游标或快照副本。先停止 VSG，再删除其 `data/` 目录可清除全部 VSG 配置、历史、日志和运行时记录。删除整个目录不可撤销，程序不会自动执行。

## 网络边界

运行时控制台只创建回环 HTTP 服务，并按需调用本机已有的 Docker/WSL、硬件识别和用户明确确认的 `llama-bench` 命令。运行时探测只访问已识别的本机回环端口；手工受信节点只允许无凭据的回环、私网或链路本地 HTTP 地址，并在连接前重新校验解析结果。模型目录是随包离线 JSON；容量规划不会访问模型链接、下载权重或发送硬件数据。安装依赖、GitHub Actions、漏洞审计和构建过程可能访问各自的软件源；这些属于开发/构建流程，不是运行时遥测。
